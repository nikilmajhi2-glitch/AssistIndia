name: assistindia
description: AssistIndia - SMS Sender (com.rupeedesk) - Flutter (Dark theme)
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase
  firebase_core: ^3.15.2
  cloud_firestore: ^5.6.12
  firebase_auth: ^5.7.0

  # ⚙️ Background + SMS (V2-safe)
  android_alarm_manager_plus: ^4.0.3
  flutter_sms_inbox: ^1.0.4
  sms_sender: ^1.0.5

  # ⚡ Utilities
  permission_handler: ^11.4.0
  shared_preferences: ^2.5.3
  provider: ^6.1.5+1
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true